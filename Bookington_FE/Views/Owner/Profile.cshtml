
@{
	ViewData["Title"] = "Profilr";
	Layout = "_LayoutOwner";
}
@using Bookington_FE.Models.ResponseModel;
@model AuthLoginResponse;


<div id="Profile" class="tabcontent" style="display:block">
			  <h2>Profile</h2>
			  <div class="container-xl px-4 mt-4">
				  <!-- Account page navigation-->
				  <hr class="mt-0 mb-4">
				  <div class="row">
					  @*<div class="col-xl-4">
						  <!-- Profile picture card-->
						  <div class="card mb-4 mb-xl-0">
							  <div class="card-header">Profile Picture</div>
							  <div class="card-body text-center">
								  <!-- Profile picture image-->
								  <img class="img-account-profile rounded-circle mb-2" src="http://bootdey.com/img/Content/avatar/avatar1.png" alt="">
								  <!-- Profile picture help block-->
								  <div class="small font-italic text-muted mb-4">JPG or PNG no larger than 5 MB</div>
								  <!-- Profile picture upload button-->
								  <button class="btn btn-primary" type="button">Upload new image</button>
							  </div>
						  </div>
					  </div>
					  <div class="col-xl-8">*@
						  <!-- Account details card-->
						  
							  <div class="card-header">Account Details</div>
							  <div class="card-body">
								  <form>									  
									  <!-- Form Row-->

							<div class="mb-3">
								<label class="small mb-1" for="inputFullName">Full name</label>
								<input class="form-control" id="inputUpUserName" type="text" placeholder="Enter user's name" value="@Model.result.fullName">
							</div>
							<div class="mb-3">
								<label class="small mb-1" for="inputFullName">Date Of Birth</label>
								<input class="form-control" id="inputUpUserDOB" type="date" placeholder="Enter user's date of birth" value="@Model.profileRead.DateOfBirth.ToString("yyyy-MM-dd")">
							</div>
									  
									  <!-- Save changes button-->
							<button class="btn btn-primary" type="button" id="OkUpdate" onclick="UpdateProfile()">Save changes</button>
								  </form>
							  </div>
						  
					  </div>
				  </div>
			  </div>		
			</div>